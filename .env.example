# Used only as a development fallback. The desktop app stores the key in
# Windows Credential Manager and never writes it into a project database.
GEMINI_API_KEY=

# Optional override for development and CI fixtures.
PAPERCRAFT_PROJECTS_DIR=
PAPERCRAFT_LOG_LEVEL=INFO
